<div class="row">
  <div class="col-md-12">
    <div class="card">


      <div class="row">
        <div class="col-md-6">
          <img class="card-img-left" src="{{viewUser.image}}" style="width: 250px;" alt="user avatar">
        </div>

        <div class="col-md-6">
          <div class="well">
            <ul class="list-unstyled">
              <li><strong>

                  <h1>{{viewUser.name}}
                    {{#ifCond viewUser.id currentUser}}

                    <a href="/users/{{viewUser.id}}/edit" class="btn btn-primary">Edit</a>

                    {{/ifCond}}
                  </h1>

                </strong></li>
              <li><strong>Email:</strong> {{viewUser.email}}</li>
              <li><strong>Role:</strong> {{#if viewUser.isAdmin}}Admin{{else}}Users{{/if}}</li>
            </ul>
          </div>
          <div>
            <h6>Followings: {{user.Followings.length}}</h6>
            <h6>Followers: {{user.Followers.length}}</h6>
            <h6>Given Comments: {{viewUser.Comments.length}}</h6>
            <h6>Favorited Restaurants: {{user.FavoritedRestaurants.length}}</h6>
          </div>

        </div>
      </div>

    </div>
  </div>
  <div class="col-md-4">
    <div class="card my-2">
      <h5 class="card-header">followings</h5>
      <div class="card-body">
        {{#each user.Followings}}
        <a href="/restaurants/{{this.id}}"><img src="{{this.image}}" alt="image-thumb" class="img-thumbnail"
            style="width:100px; height: 100px"></a>
        {{/each}}
      </div>
    </div>
  </div>
  <div class="col-md-8">
    <div class="card my-2">
      <h5 class="card-header">Given comments</h5>
      <div class="card-body">
        {{#each viewUser.Comments}}
        <a href="/restaurants/{{this.Restaurant.id}}"><img src="{{this.Restaurant.image}}" alt="image-thumb"
            class="img-thumbnail" style="width:100px; height: 100px"></a>
        {{/each}}
      </div>
    </div>
  </div>

  <div class="col-md-4">
    <div class="card my-2">
      <h5 class="card-header">followers</h5>
      <div class="card-body">
        {{#each user.Followers}}
        <a href="/restaurants/{{this.id}}"><img src="{{this.image}}" alt="image-thumb" class="img-thumbnail"
            style="width:100px; height: 100px"></a>
        {{/each}}
      </div>
    </div>
  </div>

  <div class="col-md-8">
    <div class="card my-2">
      <h5 class="card-header">Favorited</h5>
      <div class="card-body">
        {{#each user.FavoritedRestaurants}}
        <a href="/restaurants/{{this.id}}"><img src="{{this.image}}" alt="image-thumb" class="img-thumbnail"
            style="width:100px; height: 100px"></a>
        {{/each}}
      </div>
    </div>
  </div>
</div>